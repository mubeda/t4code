# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running T4Code (for example via API key or ChatGPT auth supported by Codex).
- T4Code starts the server via `codex app-server` per session.
